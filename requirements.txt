# BoxDreamer 深度学习依赖包清单
# 建议在云端服务器直接执行: pip install -r requirements.txt

# 基础数值计算与图像处理
numpy>=1.22.0
opencv-python-headless>=4.7.0.72  # 云端无桌面环境推荐使用 headless 版本，避免 libGL.so.1 缺失报错

# 深度学习框架
torch>=2.0.0
torchvision>=0.15.0

# 几何与张量操作
einops>=0.6.0
timm>=0.9.0

# 可选加速库
# flash-attn>=2.0.0

